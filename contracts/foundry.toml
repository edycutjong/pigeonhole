[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
no_match_coverage = "(script|test)/"   # coverage is measured on src/ — the deploy script and the tests are not the product
solc = "0.8.30"
evm_version = "osaka"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
arc = "https://rpc.mainnet.arc.io"

[etherscan]
arc = { key = "unused", url = "https://explorer.arc.io/api/", chain = 5042 }
